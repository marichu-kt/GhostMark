export const networkPolicy = Object.freeze({
  uploadEndpoint: null,
  analyticsProvider: null,
  externalCdnUrls: [] as string[],
  cookiesEnabled: false,
  documentPersistenceEnabled: false,
  telemetryEnabled: false,
});
